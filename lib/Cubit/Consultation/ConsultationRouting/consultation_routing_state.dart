part of 'consultation_routing_cubit.dart';

class ConsultationRoutingInitial {
  dynamic section;
  int sectionNo;
  bool sectionNotComplete;

  ConsultationRoutingInitial(
      {required this.section,
      required this.sectionNo,
      required this.sectionNotComplete});
}
